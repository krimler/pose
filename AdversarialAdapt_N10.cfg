\* TLC configuration for AdversarialAdapt module, N=10
\*
\* State space: 256 states (independent of N). Completes instantly.
\* The protocol constrains activeConfig to 3 values, so state count
\* does not grow with N.
CONSTANT
    N = 10
    MaxSwitches = 5
    MaxRounds = 20
SPECIFICATION Spec
INVARIANT MasterInvariant
